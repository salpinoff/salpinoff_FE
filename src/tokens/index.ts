import { COLORS as colors } from './colors';
import { RADIUS as borderRadius } from './radius';
import { SHADOWS as boxShadow } from './shadows';
import { SPACINGS as letterSpacing } from './spacings';

const TOKENS = {
  colors,
  borderRadius,
  boxShadow,
  letterSpacing,
};

export default TOKENS;
